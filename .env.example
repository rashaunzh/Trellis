# Optional server-side model configuration. Empty values use the published baseline.
# Copy only when needed to ignored .env.local (model scripts) or .dev.vars (local Worker).
# Use platform secrets for hosted deployments. Never commit real credentials.
# See docs/architecture/MODEL_RUNTIME.md for provider and structured-output settings.
TRELLIS_AI_PRIMARY_PROVIDER=
TRELLIS_AI_PRIMARY_BASE_URL=
TRELLIS_AI_PRIMARY_MODEL=
TRELLIS_AI_PRIMARY_API_KEY=
TRELLIS_AI_PRIMARY_STRUCTURED_OUTPUT=

TRELLIS_AI_FALLBACK_PROVIDER=
TRELLIS_AI_FALLBACK_BASE_URL=
TRELLIS_AI_FALLBACK_MODEL=
TRELLIS_AI_FALLBACK_API_KEY=
TRELLIS_AI_FALLBACK_STRUCTURED_OUTPUT=
