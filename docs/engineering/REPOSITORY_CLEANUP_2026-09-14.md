# 仓库整理交付记录

日期：2026-09-14。整理基线：`de0f9b1`。工作直接发生于现有本地 clone。

## 保留原则与实际动作

保留当前实现、迁移、可复现测试、当前产品契约、桌面点击规格、架构、关键决策和带来源的精选截图。旧版本材料退出当前树，由 Git 历史追溯。

- 基线共 543 个跟踪文件，其中 docs 150 个、memory 103 个。
- 删除 231 个已跟踪旧文件：130 个文档及产物、91 个旧记忆记录、10 个过时演示或导出脚本。
- 删除 docs 内 13 个未跟踪旧截图。
- 6 个本地文件退出版本控制并保留原文件：编辑器设置、个人偏好及四份个人路线。
- 保留 6 张有版本和操作范围说明的本地截图；删除中间文本、重复图、旧作品集导出及并行 PRD。
- 统一 README、开发/部署指引与当前进展入口；验收截图改写到 outputs，预检增加文档链接和跟踪边界检查。
- 不删除仍有依赖的兼容实现与测试。

完整删除清单见 [整理清单](repository-cleanup-manifest.json)。新增与修改以本次提交 diff 为准。

## 本地与 GitHub 边界

个人偏好、路线、机器设置、密钥、运行数据库与构建中间产物仅本地保留并忽略提交。保留运行环境和数据不等于把它们作为交付材料；新 clone 不要求拥有原机器的个人文件。

仓库维持私有。当前树清理没有擦除 Git 历史，不构成历史敏感信息已净化的承诺。未重写历史、未改变仓库可见性、未部署应用。需要查看删除前内容，可用 `git show de0f9b1:相对路径`，不要把旧目录整体恢复为第二份当前规格。

当前分支及默认 main 仅允许普通快进同步；出现远端新增提交或冲突应保留对方工作再处理。GitHub CLI 的登录失效与 Git 推送凭据分别判断；以实际远端提交号核验同步，不用文档中的声明代替验证。

## 验收与后续

执行 `npm run check` 和 `npm run delivery:precheck`，结果记入 [当前进展](PROJECT_STATUS.md)。代码测试、文件整理、桌面设计、线上验收分别报告。

后续仅维护一个当前状态文件；新截图先放 outputs，核验后带版本、场景和限制精选进入 evidence。每次功能提交同时更新对应验收状态，避免再次堆积多份“已完成”报告。
