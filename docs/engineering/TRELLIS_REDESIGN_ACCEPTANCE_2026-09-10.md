# 2026-09-10 核心重做测试执行记录

本轮已交付可运行的验收脚本，并修复测试暴露的核心流程问题。两条浏览器主链在隔离 D1 下运行通过。总体结论仍为**待验收，不放行**：产品五维评分、真实模型反馈、完整阶段出口和真人观察尚未完成。

## 实际结果

| 轮次 | 自动结果 | 原始证据目录（仓库根目录下） | 结论边界 |
|---|---|---|---|
| 判断样例 | 12例、45项通过 | `outputs/redesign-core/2026-09-10T15-04-28-870Z-judgment-18056` | 固定基线与结构约束；五维评分未填，不等于路线质量通过 |
| 浏览器 | 21项通过 | `outputs/redesign-core/2026-09-10T14-59-22-099Z-browser-26380` | 真实浏览器→服务→本地D1，含两条主链、恢复与异常分支；非真实登录 |
| 模型 | 20个计划试验完成；17次目标生成有成功调用，3次时间冲突在调用前拒绝 | `outputs/redesign-core/2026-09-10T14-51-45-566Z-model-27392` | 真实模型仅目标理解与材料判断；反馈仍为规则 |
| 工程 | `npm run check` 通过：273领域测试、6渲染测试、21迁移验证及类型/lint/构建 | `outputs/redesign-core-engineering-final.log` | 后续补充的数据库故障断言单独复测通过；不证明生产并发安全 |
| 真人 | 未执行 | [空白观察模板](TRELLIS_REDESIGN_HUMAN_OBSERVATION.md) | 不能把自动点击、截图或本报告代替真人 |

源码指纹在后续脚本中保存于 `result.json`。模型轮执行时尚无该字段，保留原记录，没有事后伪造；其后仅调整反馈原文显示、内部编号/百分比呈现及验收基础设施，未依据保留例调参。判断评分前需按当前源码重新生成证据并核对哈希。完整执行方式和覆盖矩阵见[验收入口](TRELLIS_REDESIGN_ACCEPTANCE.md)。

模型配置：Qwen `qwen3.8-flash` 主路由、GLM `glm-5.3-flash` 备用路由。20个试验累计检查耗时217441ms，27条模型尝试记录，其中5条未成功：1次材料引用无法在原文定位（needs_review）、1次超时、3次结构数组长度超限。这些输出被拒绝或进入复核，全部尝试保存在 `outputs.json`，没有只选成功结果。成功目标调用只能证明模型参与和契约通过，不能证明五维质量全部可用。

首个沙箱模型运行 `2026-09-10T14-50-45-168Z-model-13044` 失败：网络 `fetch failed`，且脚本误用了不存在的目录方法。修正脚本并在获自动批准的联网环境重新执行；旧失败目录保留，不作为模型效果样本。H1–H4 首次用于验收后不得再宣称未见过；若按其输出修复判断逻辑，补新保留集。

## 测试推动的产品修复

1. 阶段从“每门课程一个阶段”改为按前置顺序组织能力组，出口取对应能力目标；材料采用理由指出实际章节与能力映射，未采用不再泛称不相关。目录映射仍是已保存版本，不声称此次重新读取全部原文。
2. 增加拒绝调整入口，拒绝后草稿失效；已拒绝/被替代草稿不能再次确认。取消生成保留输入，迟到响应只留下待确认草稿，不覆盖正式路线。生成中刷新后仍可审阅或拒绝草稿。
3. 提交关联具体学习信号，重试复用工作流标识，修复真实D1唯一约束导致的“成功写入后重试报500”。旧标签页提交返回409，不覆盖更新反馈。
4. 跨周首次涉及新能力时，先从发布图建立运行时节点投影，修复真实SQLite/D1外键错误。新增真实SQL重开、用户隔离和中途写入故障恢复断言。
5. 学习弹窗支持焦点进入、Tab边界与Escape关闭；恢复目标输入；移除学习页内部能力编号及未经校准的百分比；任务结果引用本次原始笔记，仍明确自报不能证明掌握。

这些变化复用已有服务、决策和存储结构，没有新增公开测试API或数据库迁移，没有改个人长期路线、偏好、远端数据库或部署。

## 剩余放行条件

- 产品专家逐项评审12例以及每次模型输出的五维量规；不能让结构断言替代语义判断。
- 实际反馈当前没有调用模型，尚不能执行产品链路的“真实模型反馈”验收。需先确定并实现任务成果评价，再验证，不能用脚本另调模型冒充产品能力。
- 完整阶段收口与迁移能力出口、各类写入边界的失败恢复和跨进程并发仍未完全覆盖。已测的中途故障仅为反馈已存、行动投影尚未写完这一窗口。
- 反馈表单编辑后直接刷新时的全部未提交字段恢复尚未专项验证；当前持久化的是目标输入、保存结果及失败反馈的重试标识。
- 所有者至少三次任务、两类活动、一次隔日恢复，另有一名未参与开发者首次走查；至少一次观察→修改→复测。
- 私有部署后以真实登录身份验证保存、退出重进和恢复。当前没有重新部署，不能将本地修复描述为线上已生效。

这些缺项不是“通过但有提醒”，而是正式验收未通过的原因。本轮证明测试入口和受测的真实链路已运行，尚不证明长期学习效果或整个重做目标已完成。
