# 2026-08-20 Trellis V0.2：重排本周浏览器验收 + 部署前检查 + P0 工程计划

## 背景

用户给出明确任务书（C 盘 `trellis-cleanup` 克隆推进）：四个任务——① 手动/浏览器验收"重排本周"；② Sites 部署前检查；③ P0 功能设计落地计划（只出计划不实现）；④ 更新记忆与提交。

基线：`db94af0`（feat: add weekly replan flow），工作区干净。

## 任务一：重排本周浏览器验收（32/32 通过）

新增 `scripts/acceptance-replan.py`（Playwright 直驱，dev server 3400，系统 Python 3.14）。按任务书 10 步全流程断言：

- 入口：有状态时"重排本周"+"重新设置"均可见；点重新设置回诊断。
- 诊断：目标"我想系统学习 AI 通识，并能判断 AI 应用方案是否靠谱"、8 小时（480 分钟）、先建立全局认知 → 提案 → 确认 → 首周计划（核心 3 + 可选 2，容量 180/480 分钟）。
- 抽屉 5 项交互：步骤勾选 / 活动笔记 / 自检 3 项 / 证据类型 / 外部链接（注意：笔记/自检/证据类型/链接在 in_progress 状态才出现，planned 只有步骤+标准）。
- 活动闭环：开始 → 足量证据 → 评估通过 → 活动完成。
- 成长页：节点 `ai-literacy.mechanism` validated、中文节点名（模型机制/适用边界/上下文与提示）、相邻分支 2 个、支持证据 1 条。
- 重排本周：`activity_replan` 调整记录出现；证据 id+内容完全保留（1=1）；validated 节点与支持证据列表保留；有证据活动保留；**新增 8 个活动（5→9，因机制节点 validated 后 planner 解锁下一节点）**；4 个未产生证据的开放活动全部被替换；用户状态未清空；无 JS 错误。

结论：可作为演示版本，无必须修复项。

脚本坑（已入 skill 更新价值）：grow 页 TreeMap 容器类名是 `.t2-treemap`（旧 `.t2-node-map` 已过时，旧 browser-acceptance.py 未同步）；dev server 监听 IPv6 localhost，`curl 127.0.0.1` 不通但 `localhost` 通；`window.confirm` 用 `page.on("dialog", accept)`。

## 任务二：Sites 部署前检查

- `.openai/hosting.json` 存在：`project_id=appgprj_6a72003abefc8191a4bd0c79702ee892`、`d1="DB"`（已有 site 项目，复用不新建）。
- build artifact 完整：`dist/client` + `dist/server`（wrangler.json / ssr / assets），但 `dist/server/wrangler.json` 的 D1 绑定是 **placeholder**（`00000000-0000-4000-8000-000000000000`，database_name `site-creator-d1` 为模板默认名）；部署需 `DATABASE_ID=<真实uuid> vinext build` 重新生成。
- 远程 D1：`wrangler whoami` OAuth 已登录（rashaunzh@gmail.com，Account `33f222cb9c9aba8aa61c426e922d556d`）；`wrangler d1 list` 有 `trellis-v02-d1`（uuid `5490481c-c5a9-4423-8906-6a0d0e6e278f`，2026-08-14 创建）但 **num_tables=0，0004/0005/0006 迁移从未应用到远程**。
- 敏感信息：git 跟踪文件无 API key/token/password（0001 迁移名"secret_peter_parker"是 drizzle 随机名，内容只是 V0.1 records 加列）；`.env` 未跟踪；`.wrangler` 运行时产物未跟踪。
- 迁移对应：0004 = 学习域 12 张表，0005 = learning_profiles，0006 = learning_api_config。
- 结论：**可以进入部署**，但部署动作本身需要用户确认（不贸然创建 site / 应用远程迁移）。

## 任务三：P0 工程任务计划（已产出文档）

`docs/engineering/TRELLIS_V0.2_P0_PLAN.md`。三个关键设计决策：

1. **综合情境任务**：复用 `LearningActivity` + 新增 `activityType="synthesis_task"`（不建新表）；`learning_activities` 加 `synthesis_scope` 列（逗号分隔 nodeIds）；评估量规由 planner 写入 `evaluationCriteria`；accepted 后主节点 confidence 提升 + `weekly_light` 记录。新增枚举值（drizzle text enum 在 SQLite 无 CHECK）不需迁移，只有新列需要 0007。
2. **掌握确认**：复用 `learning_adjustments`（新增 `mastery_confirm` 类型），不新增表；节点 `NODE_STATUS` 加 `pending_confirmation`；`node_progress` 加 `confirmation_status`/`confirmed_at` 列；默认普通活动自动验证（向后兼容 42 测试），综合任务/复测驱动才需确认；纠正 → growing + confidence 降级 + 补强建议。
3. **延迟复测**：`node_progress` 加 `review_interval_days`/`next_review_at`/`review_count` 列；复测 = `activityType="retest"` 活动走现有闭环；`getWorkspace` 聚合 `dueReviews`，学习页提醒卡 + 成长页 badge 呈现（不做日历）；失败 → `evidenceInvalidated` 回 growing + 降级 + weekly_light 补强；通过 → 间隔翻倍顺延。

新增 API：`POST /api/learning/nodes/:id/confirm-mastery`、`POST /api/learning/nodes/:id/retest`；综合任务无新 API。实施顺序 5 步 + 4 个 Checkpoint（CP-A 到 CP-D，每个停让用户检查）。

## 任务四：记忆与提交

- 新增 `scripts/acceptance-replan.py`（验收脚本，可复用）。
- 新增 `docs/engineering/TRELLIS_V0.2_P0_PLAN.md`。
- 更新 `memory/handoff/current.md`、本会话记录。
- 验证：`npm run test:domain`（42/42）、eslint 0 problems、`vinext build` 通过、`node --test tests\*.test.mjs`（6/6）。
- 显式 `git add` 上述文件 → commit（51c9dae）→ push（Clash 代理）→ 同步双克隆（D 盘 + C 盘）。

## 追加：公网部署完成（2026-08-20，同日第二轮）

- 用户确认 Checkpoint 通过并放行部署。
- 远程迁移：0004/0005/0006 应用到 `trellis-v02-d1`（--remote），15 张 learning_ 表验证通过。
- `DATABASE_ID=5490481c-… vinext build` → wrangler.json 绑定真实 D1。
- `wrangler deploy` 成功：https://trellis.rashaunzh.workers.dev（账号子域 rashaunzh 由用户在 dashboard 注册），version 93c2a2bd。
- 线上验证：/product /learn /grow /workbench 200、/api/learning/workspace 200（全新空库）、/ → 307。curl 必须带浏览器 UA（Cloudflare Bot Fight Mode 403 坑）；用户浏览器可正常打开。
- 远程 D1 目前为全新空库（无用户状态）；是否跑线上闭环冒烟（写入演示数据）待用户确认。

## 下一步建议

1. 用户确认后：按 P0 计划 CP-A 开始（数据模型 + 0007 迁移）。
2. 部署：用户确认后应用远程迁移（0004/0005/0006 到 trellis-v02-d1）+ `DATABASE_ID=5490481c-… vinext build` + deploy（复用 hosting.json 的 project_id）。
