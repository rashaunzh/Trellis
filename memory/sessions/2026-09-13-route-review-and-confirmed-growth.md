# 2026-09-13 路线审阅与正式成长状态

用户要求继续重构。本轮推进完整计划与收缩计划的共同核心，不扩大课程内容，不把未回答的范围问题写成已确认决定。

## 实际改动

- 学习页首屏按目标→覆盖与缺口→第一行动→原因→材料取舍呈现。根据实际安排判断缺口，不盲信complete标签；历史记录缺少覆盖信息时明确未记录。缺口目标用名称显示，未知节点不给内部ID。
- 部分路线确认动作改为“先开始已覆盖的部分”，确认后首页仍提示限制；不再因为采用而消失。补充材料/修改目标复用原输入，调整取舍有真实锚点。后续课程和缺口细节折叠以缩短手机审阅路径。
- 成长页只读current中的confirmed路线；最新草稿不改变当前地图、阶段和课程映射。移除将记录渲染为能力等级的文案、内部信号编号和映射置信度百分比。弹窗移入焦点、限制Tab、Escape关闭并返回触发按钮。
- 沿用现有确认、服务和D1；未新增表、工作流或课程范围。并行任务的求解器和质量门修改保留，未打包成本轮实现。

## 验证

两条新增领域测试通过：实际缺口不能被complete标签掩盖；旧记录不编造覆盖结论与未知节点不泄露ID。`npm run check`通过：联合工作目录285领域测试、6渲染测试、21迁移、类型/lint/密钥扫描和构建。后续成长页与折叠呈现修改再做受影响类型/lint及浏览器验证。

27项浏览器检查通过，证据：`outputs/redesign-core/2026-09-13T00-34-20-311Z-browser-24460`。新增验证覆盖部分路线首屏、输入保留、确认与刷新后缺口仍可见、390px手机、成长弹窗焦点、待确认草稿不替换正式地图。原主链、重试、用户隔离与重启恢复继续通过。退出待验收状态，不能解释为产品发布通过。

未执行新模型质量试验或真人走查；未部署。上次提交8ebf81b的push被自动审批额度限制拒绝，不能假定已同步。当前本地改动和并行未提交文件需分别保存，避免将另一任务的成果混入同一提交。

准确下一步：继续共同核心的反馈分流，区分完成记录、自报测试和实际检查；保留旧反馈草稿并处理恢复/冲突。完整课程扩展与收缩范围仍待明确。个人路线和偏好未修改。
