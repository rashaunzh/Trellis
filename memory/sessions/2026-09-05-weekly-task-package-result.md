# 2026-09-05 周任务包与任务结果

## 本次完成

- 将周计划读模型明确为能力结果下的任务组合：核心任务、可选任务、顺序关系、完成标准和停止条件。
- 学习页展示任务类型、顺序约束与周契约，任务对象仍以能力任务为主，课程名称只作为来源信息。
- 新增 `LearningTaskResult` 服务读模型、API 和前端读取函数。
- 反馈提交后读取持久结果，展示概念、证据强度、尚未证明部分、能力变化和下一步理由。

## 产品判断

本轮沿用现有活动事实和知识状态，不新增迁移，也不把一次反馈直接等同于掌握。结果模型只解释当前证据与下一步，后续仍需通过复测或成果证据提高强度。

## 验证

- `npm run typecheck` 通过。
- `npm run lint` 通过。
- `npm run test:domain` 通过，241/241。
- `npm run build` 通过。
- `node --test tests/rendered-html.test.mjs` 通过，5/5。

## 未完成

通用内容来源拆解、工作台对象持久化关系和人工内测证据仍未完成。下一步先做人工内测，不继续堆叠页面入口。
