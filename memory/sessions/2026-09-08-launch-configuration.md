# 2026-09-08 上线配置

用户要求：做 Trellis 的上线配置。

已复用 hosting.json 所指 Sites 站点，核验已有历史发布且仅所有者可访问。线上 DB 只有 records、record_relations、weekly_reviews，records 含 0001 字段；不是旧交接所暗示的至少已有 0012。

通过 Sites 保存环境 revision 2：托管身份、精确可信主机名、所有者管理员 secret，以及现有 Qwen 主模型的五项配置，API Key 为 secret。未启用最近评估未过门的 GLM fallback。没有修改访问范围，未写入远程数据库或部署新版。

修订部署 Runbook，明确 Sites 路径、现有数据库状态、完整迁移清单、备份恢复前置和真实登录验收。迁移验证增加旧记录、关系和周复盘的保留检查。

运行配置需新版部署后才生效。本轮不将保存配置包装为上线成功，也不将表结构推断包装为已读取迁移日志。

下一步：核对托管迁移机制及已应用记录，准备可恢复的 0002–0020 升级与准确源码构建包，保持所有者范围发布并完成真实登录/模型/隔离验收。发布前应处理旧 smoke 仅伪造邮箱而非真实身份的问题。

证据：[部署 Runbook](../../docs/engineering/DEPLOYMENT_RUNBOOK.md)，Sites 只读站点/数据库响应及环境变量 revision 2。

验证：21项迁移链及旧数据保留检查、现有构建产物验证、467个已跟踪文件秘密扫描通过；环境配置已再次读回核验 revision 2。
