# 2026-08-20 Trellis V0.2：工程可用 MVP（Phase A–G 收尾）

## 背景

用户任务书（A–G 七 Phase）：把 Trellis 从"可演示 alpha"补到"工程可用 MVP"。以 Microsoft AI-For-Beginners 为可信内容样本（不重造课程），跑通闭环。内容包"先替换"。

## 完成

### A 内容样本接入（AI-For-Beginners）
- `LearningNode` 扩展：moduleId/titleEn/outcomes/sourceRefs(可点击)/activityTemplates/assessmentRubric；drizzle 迁移 0007（learning_nodes 6 新列）
- 内容包重写：ai-literacy 7 中文节点（含新增 history）+ app-dev/product 补全字段；sourceRefs 全部指向 AI-For-Beginners 真实路径（API 核实 1-Intro/2-Symbolic/3-NeuralNetworks/7-Ethics 目录 + notebook/assignment 文件名）
- validateContentPack 新增字段完整性校验；2 个新测试
- workspace 聚合注入节点中文标题；删除前端 NODE_TITLE_BY_ID 映射表（内容包单一真相）

### B 周计划编排
- 活动类型 3→6：quiz/reflection/integrated_task 新增（build_model=概念/follow_demo=阅读/independent_practice=实践）
- planner 6 类链（45/45/90/30/30/120 分钟），按节点 activityTemplates 过滤；实测 6h → 核心 6 个 360/360 分钟 6 类齐全
- 时间选项 3/5/6/8/12/15/20h + 自定义 1-20h，默认 5h
- activity-composer 补 quiz/reflection/integrated_task 模板

### C 活动抽屉 6 段
- 新增"为什么学"（节点位置/前置/后续）+ "学什么"（资源卡可点击）

### D（已达标未重做）成长页 TreeMap/中文/相邻分支/跳学验证

### E 工作台收集箱持久化
- 新表 learning_user_resources（ownerId 边界）+ 迁移 0008 + store(in-memory/d1) + service + API `/api/learning/resources/inbox` GET/POST
- **关键坑**：收集箱独立于学习状态——getWorkspace 在 profile null 时早退（userResources 硬编码 []），独立读方法 listInboxResources 解决
- 端到端验证：无 profile 可读写、刷新持久化、多 owner 隔离

### F 工程硬化
- 删除 V0.1 旧接口：/api/learning/progress、proposal 根路由、content（前端零调用）
- .gitignore 加 __pycache__/ *.pyc
- 文档 4 份：API_CONTRACT / STATE_MACHINE / LOCAL_DEVELOPMENT / DEPLOYMENT_RUNBOOK

### G 验收脚本
- 新增 scripts/acceptance-v02-main-flow.py：13/13（新用户→6h 计划→抽屉 6 段→短证据退回→修订通过→节点验证→重排保留→多 owner 隔离→reset 只清当前）
- rendered-html.test.mjs 更新（content 路由已删，改验证 build 产物路由；fileURLToPath Windows 路径坑）

## 验证（全绿）

- test:domain 51/51（含内容包 2 新测试 + owner 隔离 7）
- eslint 0 problems、vinext build 通过、node --test 6/6
- acceptance-replan.py 32/32、acceptance-v02-main-flow.py 13/13
- 收集箱端到端（无 profile/刷新/隔离）

## 遗留

- P0 三项（综合任务已有 integrated_task 活动类型+planner 支持；掌握确认/延迟复测仍是计划）
- 内容包 V1 量规/来源目录审计未做（样本已够演示）
- 远程部署待执行（本地已全绿，未重新 deploy）
- 双克隆同步待做
