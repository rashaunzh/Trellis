# 2026-08-24：AdaptiveRoutePlanner 审计与规则版 helper

## 任务

审计 Route Planner / Activity Composer 现状，设计"前半段补齐"（GoalAnalysis + CourseMaterialAnalysis + CapabilityMap → LearningRoute → WeeklyPlan → Activities → Evidence Requirements）方案；实现范围小，故补规则版 helper 与测试。约束：不改 UI、不改 DB schema、不动 Evidence Review、不引入 LangGraph、RulePlanner 保留为 fallback。

## 已确认 / 产出

1. 现状审计结论（写死位置、数据流）落文档 `docs/engineering/TRELLIS_V0.2_ADAPTIVE_ROUTE_PLANNER.md`。
2. 新增契约 `lib/learning/agents/adaptive-types.ts`：GoalAnalysis / CourseMaterialAnalysis / Capability / CapabilityMap / AdaptivePlan / AdaptivePlannerPort / ComposeAdaptiveActivityInput。
3. 新增规则实现 `lib/learning/agents/adaptive-planner.ts`：
   - `toCapabilityMap(pack)`：内容包 → 通用能力图投影（source="content_pack"），内容包保持单一数据源。
   - `validateCapabilityMap`：重复 ID / 空 signals / 环等校验。
   - `RuleAdaptiveRoutePlanner`：目标 + 前置闭包 → 拓扑排序（breadth_first 声明序 / build_first 解锁优先）→ 深度缩放活动链 → 容量填充周计划 → 信号化活动草稿；selfReport 达标能力后置/satisfied、有基础跳过基础活动。
   - `composeAdaptiveActivity`：五阶段（建立模型/阅读示范/独立练习/情境应用/复测·补强）由能力信号参数化 expectedEvidence/completionCriteria。
4. 新增测试 `tests/learning-domain/adaptive-planner.test.ts` 14 用例，覆盖最小测试清单。

## 验证

- `node --test --test-isolation=none "tests/learning-domain/*.test.ts"`：129/129 通过（原 115 + 新 14）。
- `npx tsc --noEmit`：0 错误。
- `npx eslint`（新增 3 文件）：0 problems。
- 说明：沙箱下 `node --test` 默认 spawn 子进程被拦（EPERM），用 `--test-isolation=none` 单进程运行；与代码无关。

## 未做（明确边界）

- 未接入服务层（`learning-service.ts` 的 planner 选择逻辑未改）；未改 UI / schema / Evidence Review；未引入 LangGraph；RulePlanner 与 RuleActivityComposer 原样保留。

## 下一步（建议）

- 用户决策是否把 AdaptivePlannerPort 接入服务层（诊断/确认/重排选择逻辑），以及 GoalAnalysis / CourseMaterialAnalysis 的 AI 生成器接口定义。
