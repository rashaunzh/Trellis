# 2026-08-22 V0.2 云端仓库整理

## 背景

用户确认 Trellis 已开发到 V0.2，需要整理 GitHub 云端仓库 `rashaunzh/AI-Learning-OS`。本地 `main` 起初落后 `origin/main` 38 个提交，并带有基于旧 V0.1 工作树的 Day 1 实验改动。

## 处理

- 将旧 V0.1 实验改动暂存为 `stash@{0}: codex-v01-day1-mvp-before-v02-sync`，避免混入 V0.2。
- 将本地 `main` 快进到 `origin/main` 最新提交 `ae05349`。
- 确认远端默认分支 `main` 与 `docs/trellis-v02-adaptive-learning-prd` 指向同一 V0.2 最新提交。
- 清理本地 dev server 临时日志，使工作区重新可控。
- 将 npm 元数据版本从 `0.1.0` 更新为 `0.2.0`。
- 更新 `memory/handoff/current.md`：后续以 GitHub 默认分支 `main` 为唯一交付源。

## 验证

- `npm run test:domain`：60/60 通过。
- `node_modules\.bin\vinext.cmd build`：通过，路由包含 `/learn`、`/grow`、`/workbench`、`/product` 与 V0.2 learning API。
- `node --test tests\*.test.mjs`：6/6 通过。
- `node_modules\.bin\eslint.cmd . --ignore-pattern dist --ignore-pattern .next`：0 problems。

## 注意

- `npm run test` 仍依赖 `bash scripts/build-verified.sh`；当前 Windows 环境的 `bash` 指向 WSL，容易因 WSL 环境缺少 `/bin/bash` 或 `node` 失败。Windows 下优先使用本记录中的原生命令完成等价验证。
- 旧 V0.1 Day 1 实验改动只是历史临时实现，不应直接套到 V0.2 主线。
